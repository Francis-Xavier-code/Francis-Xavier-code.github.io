# 网站维护指南

这是一个 Hugo + PaperMod 静态博客。推送到 `main` 分支后，GitHub Actions 会自动构建并发布到 GitHub Pages。

## 写文章

文章位于 `content/post/<slug>/index.md`。同一目录可存放文章图片，并在 Markdown 中直接引用文件名。

```yaml
---
title: 我的第一篇文章
date: 2026-07-11
slug: my-first-post
description: 一句话摘要
categories: [技术]
tags: [Hugo]
draft: false
---
```

## 调整网站

- 全局配置：`hugo.yaml`
- 自定义样式：`assets/css/extended/site.css`
- 自定义模板：`layouts/`
- 关于页：`content/about/index.md`

首页展示最新文章；顶部仅保留文章、关于和搜索。文章、关于和站点配置也可通过 `.pages.yml` 定义的管理界面维护。

## 发布

```bash
git add .
git commit -m "更新网站"
git push
```

本地预览可运行：

```powershell
.\.tools\hugo\hugo.exe server -D
```

或双击 `5-本地管理后台.bat`，使用精简的文章管理与发布页面。
