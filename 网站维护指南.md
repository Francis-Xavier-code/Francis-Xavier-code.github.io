# 🚀 XYNRIN BLOG 维护与使用指南 (PaperMod 版)

这篇文档为你记录了博客当前的架构、后续怎么写文章、以及如何修改各种配置，请妥善保存以便日后查阅！

---

## 1. 📝 如何写文章？

### 1.1 文章的存放位置
博客的所有文章都放在 `content/post/` 目录下。

**强烈推荐使用「文件夹（Page Bundle）」的方式来写文章**：
为了方便在本地编辑器（如 Typora、VS Code、Obsidian）里直接肉眼预览图片，你应该为每一篇文章创建一个**同名文件夹**。

例如，你要写一篇叫《我的第一篇文章》的博客：
1. 在 `content/post/` 下新建文件夹：`content/post/my-first-post/`
2. 在该文件夹里创建 `index.md`，在这个文件里写正文。
3. 把这篇文章用到的所有图片（如 `01.png`），直接放在这个文件夹里。
4. 在 Markdown 里引用图片时，**直接写图片名**：`![图片描述](01.png)`

这样，你在本地 Markdown 软件里就能完美看到图片，推送到线上也会完美显示！

### 1.2 文章的头部配置 (Front Matter)
每篇文章的 `index.md` 开头必须包含配置（用 `---` 包围）：

```yaml
---
title: 我的第一篇文章
date: 2026-06-22
slug: my-first-post
description: 这里是文章的简短摘要
cover:
  image: 01.png  # 直接写当前文件夹里的图片名，作为文章封面图
categories:
  - 技术
tags:
  - 教程
draft: false  # 改为 true 就会隐藏文章
---
这里开始写正文...
```

---

## 2. ⚙️ 那些地方怎么改？(网站配置修改)

网站的全局配置都在根目录下的 **`hugo.yaml`** 文件里，这里列出了最常用的几个修改点：

### 2.1 修改主页个人名片 (Profile Mode)
打开 `hugo.yaml`，找到 `profileMode` 节点：
```yaml
  profileMode:
    enabled: true
    title: Xynrin         # 主页显示的大标题
    subtitle: 记录折腾... # 标题下方的小字，支持 <br/> 换行
    imageUrl: /img/avatar.png  # 你的大头像，放在 static/img/avatar.png 里
```

### 2.2 修改底部的社交图标
在 `hugo.yaml` 的 `socialIcons` 节点下增删：
```yaml
  socialIcons:
    - name: github
      url: https://github.com/Xynrin
    - name: email
      url: mailto:xynrin@163.com
    - name: bilibili
      url: https://space.bilibili.com/1198508132
```
*注：`name` 必须是小写英文字母，PaperMod 会自动根据 name 去匹配对应的图标（如 x, twitter, facebook, telegram 等）。*

### 2.3 修改顶部菜单栏
在 `hugo.yaml` 最底部找到 `menu.main` 节点：
```yaml
menu:
  main:
    - identifier: posts
      name: 文章       # 这里改你想显示的文字
      url: /post/
      weight: 10      # weight 越小越排在前面
```

### 2.4 修改底部运行时间与访客记录
这些功能是通过“局部注入”的方式写在自定义页脚的。
如果你想修改底部统计文字或者隐藏它，请去打开：
👉 `layouts/partials/extend_footer.html` 
里面的代码非常直白，直接修改文本内容即可。

### 2.5 修改 Changelog 内容
我在你网站底部加了 Changelog 的链接。
它的内容放在 👉 `content/changelog.md` 里面，当你博客有了新改动，可以直接去这个 md 文件里面加一行记录。

---

## 3. ☁️ 如何发布/更新网站？

你使用的是 GitHub Pages 和 GitHub Actions。所以只要你把本地的修改推送到 GitHub 上，就会自动完成编译和发布！

日常写完文章后，依次执行：
```bash
git add .
git commit -m "add: new post"
git push
```
然后喝杯茶，等 1~2 分钟，刷新你的 `https://xynrin.github.io` 就能看到最新效果啦！
